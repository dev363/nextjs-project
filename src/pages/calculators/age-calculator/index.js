import { Container } from "react-bootstrap";
import AgeCalculator from "components/age-calculator";

export default () => {
  return (
    <Container>
      <AgeCalculator />
    </Container>
  );
};
